<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Şifre Sıfırla</title>
    <link rel="icon" href="/images/favicon.ico" type="image/x-icon">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
</head>
<body class="bg-gray-50">
    <%- include('partials/header') %>

    <div class="min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
        <div class="max-w-md w-full space-y-8 bg-white p-8 rounded-xl shadow-lg">
            <h1 class="text-center text-2xl font-bold text-gray-700">Şifre Sıfırla</h1>
            
            <% if (error) { %>
                <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
                    <span class="block sm:inline"><%= error %></span>
                </div>
            <% } %>

            <% if (success) { %>
                <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative" role="alert">
                    <span class="block sm:inline"><%= success %></span>
                    <div class="mt-4 text-center">
                        <a href="/giris" class="text-sm text-yellow-600 hover:text-yellow-700">
                            Giriş sayfasına dön
                        </a>
                    </div>
                </div>
            <% } else if (token) { %>
                <form action="/auth/reset-password/<%= token %>" method="POST" class="mt-8 space-y-6">
                    <div>
                        <label for="password" class="block text-sm font-medium text-gray-700">Yeni Şifre</label>
                        <input 
                            type="password" 
                            name="password"
                            id="password" 
                            class="mt-1 block w-full px-3 py-2 bg-white border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-yellow-500 focus:border-yellow-500"
                            required
                        >
                    </div>
                    <div>
                        <label for="confirmPassword" class="block text-sm font-medium text-gray-700">Şifre Tekrar</label>
                        <input 
                            type="password" 
                            name="confirmPassword"
                            id="confirmPassword" 
                            class="mt-1 block w-full px-3 py-2 bg-white border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-yellow-500 focus:border-yellow-500"
                            required
                        >
                    </div>
                    <div>
                        <button type="submit" class="w-full py-3 px-4 bg-yellow-600 hover:bg-yellow-700 text-white font-medium rounded-md shadow-sm transition-colors">
                            Şifremi Sıfırla
                        </button>
                    </div>
                </form>
            <% } else { %>
                <div class="mt-4 text-center">
                    <a href="/giris" class="text-sm text-yellow-600 hover:text-yellow-700">
                        Giriş sayfasına dön
                    </a>
                </div>
            <% } %>
        </div>
    </div>

    <%- include('partials/footer') %>
</body>
</html> 